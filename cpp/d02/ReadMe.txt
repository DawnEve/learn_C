	a4 static 成员变量
	a5 指针与多维数组
	a6 多维数组，数组名+1后移动了多少？
	a7 返回 指向一维（char）数组的指针 的函数: 指向一维数组的指针
	a8 返回 指向一维（int）数组的指针 的函数
		* 1d array: a8_1d.cpp
		* 2d array: a8_2d.cpp
		* 3d array: a8_3d.cpp
	a9 定义返回 指向2d 数组的指针 的函数，指向3d数组的指针的 函数，接收返回值，并打印该2d数组
		* 定义返回 指向3d数组的指针 的函数: int (* get3d(int index) ) [4][2]; //返回指向3d数组的指针
			- 声明变量接收起返回值: int (*pArr2d)[4][2] = arr3d; 
			- int (*pArr3)[4][2] = get3d(2); //还是一个3d数组指针。同原数组名同一个级别的。
		* 定义返回 指向2d数组的指针 的函数: int (* get2d(int index) )[2]; //返回指向2d数组的指针
			- 声明变量接收返回值: 数组名变指针，本质上是指向其内部的第一个元素。
			- int (*pArr2)[2] = get2d(1); //和数组名同一个级别的指针，看定义，会损失一个维度。
	a9_3d_simple.cpp 一个测试题: 
		定义一个返回 指向2d 数组的指针 的函数，并定义变量接收返回值。使用该返回值打印该2d数组
	a10 数组的两种表示形式 char *week[10]={}; char week[7][10]={};
		* 元素是指针的，指向的是静态区
		* 元素是数组的，是原始字符串的拷贝
		
			